{
  "name": "claude-core-cli",
  "version": "0.2.0",
  "type": "module",
  "scripts": {
    "start": "npx tsx cli.ts",
    "build:rust": "cd ../claude-core && cargo build --release"
  },
  "dependencies": {
    "tsx": "^4.19.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
